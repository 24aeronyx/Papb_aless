import Colors from "../../styles/colors"
import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    searchArea: {
        backgroundColor: Colors.searchBares,
        borderRadius: 10,
        marginBottom: 10
    }
})

export default style;
